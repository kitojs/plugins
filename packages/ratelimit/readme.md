# @kitojs/ratelimit

Ratelimit middleware for the Kito framework

## Installation

```bash
pnpm add @kitojs/ratelimit
```

## Usage

```ts
import { ratelimit } from "@kitojs/ratelimit";

app.use(ratelimit());
```
